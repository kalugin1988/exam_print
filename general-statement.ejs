<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Общая ведомость учета олимпиадных работ</title>
    <style>
        body {
            font-family: 'Times New Roman', serif;
            margin: 0;
            padding: 20px;
        }
        .statement {
            max-width: 800px;
            margin: 0 auto;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
        }
        .header h1 {
            font-size: 16px;
            font-weight: normal;
            margin-bottom: 30px;
        }
        .info-line {
            margin-bottom: 20px;
            font-size: 14px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            font-size: 12px;
            margin-bottom: 30px;
        }
        th, td {
            border: 1px solid black;
            padding: 8px;
            text-align: center;
            vertical-align: middle;
        }
        th {
            font-weight: bold;
        }
        .footer {
            margin-top: 50px;
            font-size: 14px;
        }
        .signature-line {
            margin-top: 30px;
            text-align: right;
        }
        .print-button {
            position: fixed;
            top: 20px;
            right: 20px;
            background-color: #007bff;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
        @media print {
            .print-button {
                display: none;
            }
            body {
                padding: 0;
            }
        }
    </style>
</head>
<body>
    <button class="print-button" onclick="window.print()">Печать</button>
    
    <div class="statement">
        <div class="header">
            <h1>Общая ведомость учета олимпиадных работ</h1>
        </div>

        <div class="info-line">
            <span>Предмет: <strong><%= subject %></strong></span>
        </div>

        <div class="info-line">
            <span>Дата проведения Олимпиады: «<%= new Date(exam_date).getDate() %>» 
                  <%= new Date(exam_date).toLocaleDateString('ru-RU', { month: 'long' }) %> 
                  <%= new Date(exam_date).getFullYear() %> г.</span>
        </div>

        <div class="info-line">
            <span>Код площадки: <strong><%= site_code %></strong></span>
        </div>

        <table>
            <thead>
                <tr>
                    <th width="15%">№ аудитории</th>
                    <th width="15%">Параллель</th>
                    <th width="20%">Планируемое кол-во работ</th>
                    <th width="20%">Фактическое кол-во работ</th>
                    <th width="15%">Ответственный организатор в аудитории</th>
                    <th width="15%">Подпись организатора в аудитории</th>
                </tr>
            </thead>
            <tbody>
                <% rows.forEach((row, index) => { %>
                <tr>
                    <td><%= row.classroom %></td>
                    <td><%= row.parallel %></td>
                    <td><%= row.planned %></td>
                    <td><%= row.actual %></td>
                    <td><%= row.responsible %></td>
                    <td><%= row.signature %></td>
                </tr>
                <% }); %>
            </tbody>
        </table>

        <div class="footer">
            <div class="signature-line">
                Ответственный в штабе ППО: _________ / ___________________________
            </div>
        </div>
    </div>
</body>
</html>