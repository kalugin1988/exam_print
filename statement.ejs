<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ведомость учета сданных работ</title>
    <style>
        body {
            font-family: 'Times New Roman', serif;
            margin: 0;
            padding: 20px;
        }
        .statement {
            max-width: 800px;
            margin: 0 auto;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
        }
        .header h1 {
            font-size: 16px;
            font-weight: normal;
            margin-bottom: 30px;
        }
        .info-line {
            display: flex;
            justify-content: space-between;
            margin-bottom: 20px;
            font-size: 14px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            font-size: 12px;
            margin-bottom: 30px;
        }
        th, td {
            border: 1px solid black;
            padding: 5px;
            text-align: center;
            vertical-align: middle;
        }
        th {
            font-weight: bold;
        }
        .student-name {
            text-align: left;
            min-width: 200px;
        }
        .footer {
            margin-top: 50px;
            text-align: right;
            font-size: 14px;
        }
        .print-button {
            position: fixed;
            top: 20px;
            right: 20px;
            background-color: #007bff;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
        .print-button:hover {
            background-color: #0056b3;
        }
        @media print {
            .print-button {
                display: none;
            }
            body {
                padding: 0;
            }
        }
    </style>
</head>
<body>
    <button class="print-button" onclick="window.print()">Печать</button>
    
    <div class="statement">
        <div class="header">
            <h1>Ведомость учета сданных работ в аудитории</h1>
        </div>

        <div class="info-line">
            <span>Предмет: <strong><%= subject %></strong></span>
        </div>

        <div class="info-line">
            <span>Дата проведения Олимпиады: «<%= new Date(exam_date).getDate() %>» 
                  <%= new Date(exam_date).toLocaleDateString('ru-RU', { month: 'long' }) %> 
                  <%= new Date(exam_date).getFullYear() %> г.</span>
        </div>

        <div class="info-line">
            <span>Код площадки: <strong><%= site_code %></strong></span>
            <span>№ аудитории: <strong><%= classroom %></strong></span>
        </div>

        <table>
            <thead>
                <tr>
                    <th width="5%">№ п/п</th>
                    <th width="30%">ФИО участника</th>
                    <th width="10%">Рабочее место</th>
                    <th width="15%">Параллель заданий</th>
                    <th width="15%">Кол-во листов в работе</th>
                    <th width="25%">Подпись о сдаче работы</th>
                </tr>
            </thead>
            <tbody>
                <% students.forEach((student, index) => { %>
                <tr>
                    <td><%= index + 1 %></td>
                    <td class="student-name"><%= student.fullName %></td>
                    <td><%= student.workplace %></td>
                    <td><%= student.parallel %></td>
                    <td><%= student.sheets %></td>
                    <td><%= student.signature %></td>
                </tr>
                <% }); %>
            </tbody>
        </table>

        <div class="footer">
            Организатор в аудитории: ________ / ______________________
        </div>
    </div>
</body>
</html>